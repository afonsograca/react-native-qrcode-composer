import type React from 'react';
import type {SvgProps} from 'react-native-svg';

const SvgMock: React.FunctionComponent<SvgProps> = () => null;

export const ReactComponent = SvgMock;
export default SvgMock;
